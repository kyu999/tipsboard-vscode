- 実装前に、関連するREADME、仕様書、既存コード、テストを確認し、現在の設計方針・命名規則・責務分離に従うこと。
- 要件が曖昧な場合は、推測で大きく実装せず、前提・選択肢・影響範囲を整理して確認すること。
- 変更範囲は目的達成に必要な最小限に留め、無関係なリファクタリングやフォーマット変更を混ぜないこと。
- 既存の仕組み・ライブラリ・ヘルパーを優先し、同じ目的の実装を重複して作らないこと。
- 新しい抽象化は、重複削減・責務分離・変更容易性に明確に役立つ場合のみ追加すること。
- 一時しのぎの実装、過度に複雑な実装、将来の保守者が意図を読み取りにくい実装を避けること。
- エラー処理、境界値、空データ、失敗時の挙動を考慮し、正常系だけで実装を完了しないこと。
- 仕様や外部インターフェースを変更する場合は、互換性・移行方法・影響範囲を確認すること。
- データ構造、設定、永続化形式、API契約などを変更する場合は、関連する呼び出し元・利用箇所・テストも整合させること。
- 実装を変更した場合は、必要に応じてREADME、仕様書、コメント、サンプル、運用手順も更新すること。
- テストは変更のリスクに応じて追加・更新し、既存のテスト方針に合わせること。
- セキュリティ上重要な情報、認証情報、個人情報、環境依存の秘密情報をコードやドキュメントに含めないこと。
- 実装後は、変更内容、確認した動作、実行したテスト、残っている懸念点を簡潔に共有すること。