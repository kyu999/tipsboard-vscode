{
  "name": "tipsboard-vscode",
  "displayName": "Tipsboard",
  "description": "Markdown wiki and personal knowledge management workspace",
  "version": "0.3.0",
  "publisher": "tipsboard",
  "icon": "icon.png",
  "galleryBanner": {
    "color": "#1e1e1e",
    "theme": "dark"
  },
  "author": {
    "name": "kyu999",
    "url": "https://github.com/kyu999"
  },
  "license": "Apache-2.0",
  "homepage": "https://kyu999.github.io/tipsboard-vscode/",
  "repository": {
    "type": "git",
    "url": "https://github.com/kyu999/tipsboard-vscode.git"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
    "tipsboard",
    "vscode",
    "markdown",
    "markdown editor",
    "markdown wiki",
    "personal wiki",
    "wiki",
    "wikilinks",
    "internal links",
    "backlinks",
    "note taking",
    "pkm",
    "knowledge base",
    "knowledge management",
    "zettelkasten",
    "second brain",
    "local-first",
    "offline",
    "vault",
    "tags",
    "kanban",
    "mermaid",
    "latex",
    "katex",
    "math",
    "documentation",
    "file attachments",
    "semantic search"
  ],
  "activationEvents": [
    "onCommand:tipsboard-vscode.open",
    "onCommand:tipsboard-vscode.newNote",
    "onCommand:tipsboard-vscode.closeEditorTab",
    "onCommand:tipsboard-vscode.downloadSemanticRuntime",
    "onCommand:tipsboard-vscode.installSemanticRuntime"
  ],
  "main": "./dist/extension/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "tipsboard-vscode.open",
        "title": "Tipsboard: Open"
      },
      {
        "command": "tipsboard-vscode.selectVaultFolder",
        "title": "Tipsboard: Select Vault Folder..."
      },
      {
        "command": "tipsboard-vscode.newNote",
        "title": "Tipsboard: New Note"
      },
      {
        "command": "tipsboard-vscode.closeEditorTab",
        "title": "Tipsboard: Close active tab"
      },
      {
        "command": "tipsboard-vscode.downloadSemanticRuntime",
        "title": "Tipsboard: Download Semantic Runtime"
      },
      {
        "command": "tipsboard-vscode.installSemanticRuntime",
        "title": "Tipsboard: Install Semantic Runtime from File..."
      }
    ],
    "keybindings": [
      {
        "command": "tipsboard-vscode.newNote",
        "key": "ctrl+n",
        "mac": "cmd+n",
        "when": "activeWebviewPanelId == 'tipsboard-vscode.main'"
      },
      {
        "command": "tipsboard-vscode.closeEditorTab",
        "key": "ctrl+alt+shift+w",
        "mac": "cmd+alt+shift+w",
        "when": "activeWebviewPanelId == 'tipsboard-vscode.main'"
      }
    ],
    "configuration": {
      "title": "Tipsboard",
      "properties": {
        "tipsboard-vscode.vaultFolder": {
          "type": "string",
          "default": null,
          "description": "When using a multi-root workspace, vault root folder name matching a workspace folder, or absolute path.",
          "scope": "resource"
        },
        "tipsboard-vscode.manualVaultPath": {
          "type": "string",
          "default": "",
          "markdownDescription": "Absolute vault path from *Tipsboard: Select Vault Folder* or typed here. When set, overrides the workspace folder (including single-folder workspaces). Clear to use the open folder as the vault again.",
          "description": "Absolute vault path. Overrides workspace folder when non-empty. Clear to use the workspace folder."
        },
        "tipsboard-vscode.maxAttachmentBytes": {
          "type": "number",
          "default": 10485760,
          "minimum": 1,
          "maximum": 1073741824,
          "markdownDescription": "Maximum size in bytes for each dropped attachment (images and other files). Default is 10 MiB.",
          "description": "Maximum attachment size in bytes per file."
        },
        "tipsboard-vscode.semanticSearch.provider": {
          "type": "string",
          "enum": [
            "off",
            "bundled"
          ],
          "default": "bundled",
          "markdownDescription": "Semantic search (meaning-based search from the wand button in the Tipsboard panel).\n\n- **bundled** (default): local Transformers.js runtime prepared in VS Code global storage. If missing, Tipsboard can download or install a runtime pack.\n- **off**: disables semantic search.\n\nOptional `importedPath` overrides where `@huggingface/transformers` is resolved from.",
          "description": "Semantic search on/off."
        },
        "tipsboard-vscode.semanticSearch.modelId": {
          "type": "string",
          "default": "Xenova/paraphrase-multilingual-MiniLM-L12-v2",
          "markdownDescription": "Hugging Face model id for local semantic search (Transformers.js). Usually leave the default.",
          "description": "Semantic search model id."
        },
        "tipsboard-vscode.semanticSearch.importedPath": {
          "type": "string",
          "default": "",
          "markdownDescription": "Optional absolute path to a prepared semantic runtime folder (e.g. unzipped output of `npm run prepare:semantic-pack`). When set, it is used instead of the managed runtime in global storage. Must contain `manifest.json` and `node_modules/@huggingface/transformers`.",
          "description": "Local semantic search engine folder."
        },
        "tipsboard-vscode.semanticSearch.runtimeDownloadBaseUrl": {
          "type": "string",
          "default": "https://github.com/kyu999/tipsboard-vscode/releases/latest/download",
          "markdownDescription": "Base URL used to download Tipsboard semantic runtime packs. The extension appends `tipsboard-semantic-runtime-<platform>-<arch>.zip`.",
          "description": "Semantic runtime pack download base URL."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile && npm run build:extension && npm run build:webview",
    "compile": "tsc --noEmit -p ./tsconfig.extension.json",
    "build:extension": "node scripts/build-extension.cjs",
    "build:extension:fast": "TIPSBOARD_SKIP_SEMANTIC_COPY=1 node scripts/build-extension.cjs",
    "prepare:semantic-pack": "node scripts/prepare-semantic-pack.cjs",
    "watch": "tsc -watch -p ./tsconfig.extension.json",
    "watch:webview": "npm run watch --prefix webview",
    "build:webview": "npm install --prefix webview && npm run build --prefix webview",
    "build:webview:fast": "npm run build --prefix webview",
    "lint": "tsc --noEmit -p ./tsconfig.extension.json",
    "test": "vitest run --config vitest.config.mts",
    "test:webview:e2e": "playwright test --config playwright.config.ts",
    "package": "node scripts/package-vsix.cjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/adm-zip": "^0.5.8",
    "@types/jsdom": "^21.1.7",
    "@types/node": "^20.17.14",
    "@types/vscode": "^1.85.0",
    "@vscode/vsce": "^3.9.1",
    "esbuild": "^0.28.0",
    "jsdom": "^25.0.1",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  },
  "private": true,
  "optionalDependencies": {
    "@huggingface/transformers": "^4.2.0"
  },
  "dependencies": {
    "adm-zip": "^0.5.17"
  }
}
